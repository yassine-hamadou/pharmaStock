{% extends 'dashboard/dashboardBase.html.twig' %}

{% block title %}Categorie de produit{% endblock %}

{% block dashboardSection %}
    <h1>Categories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_type.id }}</td>
            </tr>
            <tr>
                <th>NomCategory</th>
                <td>{{ product_type.nomCategory }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_type_index') }}">Retour</a>

    <a href="{{ path('app_product_type_edit', {'id': product_type.id}) }}">edit</a>

    {{ include('product_type/_delete_form.html.twig') }}
{% endblock %}
